version: '3.8'

services:
  cursos-postgres:
    container_name: gestao_cursos_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=cursos
